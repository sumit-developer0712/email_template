import styled from 'styled-components';
import { LOWEL_DARK_GRAY } from '../../../../shared/constants';

export const StyledSectionHeader = styled.div`
  background-color: ${LOWEL_DARK_GRAY};
  label {
    margin: 0 0 0 12px;
  }
`;